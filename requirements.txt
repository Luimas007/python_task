# ---- API ---------------------------------------------------------------
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
websockets==16.1.1
httpx==0.27.2
python-dotenv==1.0.1

# ---- PostgreSQL --------------------------------------------------------
psycopg2-binary==2.9.9

# ---- Scraping ----------------------------------------------------------
# curl_cffi impersonates a real Chrome TLS fingerprint; plain requests is
# rejected by GSMArena.
curl_cffi==0.16.2
beautifulsoup4==4.12.3
lxml==6.1.2

# ---- RAG ---------------------------------------------------------------
# CPU wheels are sufficient: the GPU is reserved for the Ollama LLM.
sentence-transformers==3.1.1
torch==2.13.0
numpy==1.26.4

# ---- Tests -------------------------------------------------------------
pytest==9.1.1
selenium==4.48.0
